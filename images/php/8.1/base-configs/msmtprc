# Default email account configuration
account default

# SMTP relay server (Mailcatcher for local development)
host mailcatcher

# Port for the SMTP server (default for Mailcatcher)
port 1025

# Sender email address
from "user@domain.com"
