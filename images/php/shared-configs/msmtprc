# Default account configuration for msmtp
# This configuration directs emails to the local mailcatcher service for testing purposes.

account default
host mailcatcher        # Specify the SMTP server (local mailcatcher instance).
port 1025               # Specify the port for mailcatcher.
from "user@domain.com"  # The default sender email address used in the From header.
