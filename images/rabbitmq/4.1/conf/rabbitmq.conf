#-----------------------------------------------------------------------------
# RABBITMQ MEMORY CONFIGURATION
#-----------------------------------------------------------------------------
# Configure memory usage limits to prevent RabbitMQ from consuming excessive RAM
# This setting triggers flow control when memory usage reaches the threshold

# Set absolute memory high watermark to 1GB
# When this limit is reached, RabbitMQ will block publishers to prevent OOM
vm_memory_high_watermark.absolute = 1GB
